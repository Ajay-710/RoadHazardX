rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allowing read and write as we use Clerk for UI auth
      // Bridge Clerk and Firebase for production security
      allow read, write: if true;
    }
  }
}

